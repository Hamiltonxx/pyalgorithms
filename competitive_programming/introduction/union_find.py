# The Find operation finds the subset(representative) of a given element.
# The Union operation merges two subsets if they belong to the same set(representative).
# Use the concept of trees. The root acts as a representative, and each node holds the reference to its parent node.
